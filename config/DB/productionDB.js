module.exports = {
    mongoDBUrl: 'the url to the mlab database goes here' //Note that in real production, this url should be addded in heroku env and then used here (process.env.MONGO_DB_URL, where mongo db url is the env created in heroku)
}